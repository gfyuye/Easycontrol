name: Build Android APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1. 检出代码
    - name: Checkout code
      uses: actions/checkout@v4

    # 2. 关键修改：设置 JDK 17（Android Gradle Plugin 8.2+ 要求）
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'  # 从 '11' 改为 '17'
        distribution: 'temurin'

    # 3. 缓存 Gradle 依赖
    - name: Cache Gradle dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('easycontrol/**/*.gradle*', 'easycontrol/gradle/wrapper/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    # 4. 授予执行权限并构建 Debug APK
    - name: Build APK with Gradle
      run: |
        cd easycontrol
        chmod +x ./gradlew
        ./gradlew assembleDebug --stacktrace

    # 5. 查找生成的 APK 文件
    - name: Find APK File
      id: find-apk
      run: |
        APK_PATH=$(find easycontrol -name "*.apk" -type f | head -1)
        if [ -n "$APK_PATH" ]; then
          echo "apk_path=$APK_PATH" >> $GITHUB_OUTPUT
          echo "✅ 找到 APK: $APK_PATH"
          ls -la $(dirname "$APK_PATH")/
        else
          echo "❌ 未找到 APK 文件"
          echo "正在搜索所有可能位置..."
          find easycontrol -type f -name "*.apk"
          exit 1
        fi

    # 6. 上传 APK 作为工作流制品
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: easycontrol-debug-apk
        path: ${{ steps.find-apk.outputs.apk_path }}
        retention-days: 7

    # 7. 构建成功提示
    - name: Build Success
      if: success()
      run: |
        echo "🎉 Android APK 构建成功！"
        echo "请点击上方的 'Summary' 选项卡，在 'Artifacts' 区域下载 APK 文件。"
